
export const agentReducer = (state = {}, action) => {
    //   switch (action.type) {
    //     case SEARCH_ACTION:
    //       return { ...state, books: action.payload };
    //     default:
    //       return state;
    //   }
    return state;
    };
    